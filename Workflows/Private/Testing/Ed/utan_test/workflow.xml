<?xml version='1.0' encoding='UTF-8'?>
<workflow xmlns="http://vmware.com/vco/workflow" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://vmware.com/vco/workflow http://vmware.com/vco/workflow/Workflow-v4.xsd" root-name="item1" object-name="workflow:name=generic" id="4e448126-7345-4fb1-8bb6-2a59a4b5a98e" version="0.0.0" api-version="6.0.0" allowed-operations="vfe" restartMode="1" resumeFromFailedMode="0">
  <display-name><![CDATA[utan_test]]></display-name>
  <position y="50.0" x="100.0"/>
  <input>
    <param name="utan" type="string"/>
  </input>
  <workflow-item name="item0" type="end" end-mode="0">
    <position y="45.40909090909091" x="384.5"/>
  </workflow-item>
  <workflow-item name="item1" out-name="item0" catch-name="item0" throw-bind-name="NULL" type="task">
    <display-name><![CDATA[Scriptable task]]></display-name>
    <script encoded="false"><![CDATA[//Get etcd "get" Rest Operation
var getHosts = Server.findAllForType("REST:RESTOperation")
for(var i =0; i < getHosts.length ; i++){
	if(getHosts[i].name == "Unified Compute etcd GET"){
		var restOp = getHosts[i]
			System.log("Name - " + restOp.name)
		}
}
var returnValue = [];
if(utan.length > 4){
	//Set etcd key path
	//content = "unifiedcompute/servers/" + utan + "/environment"
	content = "iAmpData/" + utan + "/name"
	var inParamtersValues = [content];
	var request = restOp.createRequest(inParamtersValues);
	
	//set the request content type
	request.contentType = "application\/json";
	System.log("Request: " + request);
	System.log("Request URL: " + request.fullUrl);
	
	var response = request.execute();
	
	//prepare output parameters
	System.log("Response: " + response);
	statusCode = response.statusCode;
	statusCodeAttribute = statusCode;
	System.log("Status code: " + statusCode);
	contentLength = response.contentLength;
	headers = response.getAllHeaders();
	contentAsString = response.contentAsString;
	System.log("Content as string: " + contentAsString);
	var keyValue = JSON.parse(contentAsString);
	if(statusCode == '200'){
		System.log("Key Value = " + keyValue.node.value);
		var returnValue = [];
		returnValue.push(keyValue.node.value);
		System.log(returnValue)
	}
	else{
		var empty = "Please enter a valid UTAN above.";
		var returnValue = [];
		returnValue.push(empty);
		System.log(returnValue)
	}
	
} else {
	var empty = "Please enter a valid UTAN above.";
	var returnValue = [];
	returnValue.push(empty);
	System.log(returnValue)
}]]></script>
    <in-binding>
      <bind name="utan" type="string" export-name="utan"/>
    </in-binding>
    <out-binding/>
    <position y="55.90909090909091" x="205.0"/>
  </workflow-item>
  <presentation>
    <p-param name="utan">
      <desc><![CDATA[utan]]></desc>
    </p-param>
  </presentation>
</workflow>